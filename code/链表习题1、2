
🟢 题目一：合并两个有序链表

📌 题目描述

将两个按非递减顺序排列的链表合并为一个新的有序链表。

示例：

输入：
1 2 4 -1
1 3 4 -1

输出：
1 1 2 3 4 4


⸻

💻 完整代码（可直接运行）

#include <stdio.h>
#include <stdlib.h>

// 定义链表节点
typedef struct Node{
    int data;
    struct Node* next;
} Node;

// 创建链表（尾插法）
Node* createList(){
    Node* head = NULL;
    Node* tail = NULL;
    int x;

    while(scanf("%d", &x) && x != -1){
        Node* node = (Node*)malloc(sizeof(Node));
        node->data = x;
        node->next = NULL;

        if(head == NULL){
            head = tail = node;  // 第一个节点
        } else {
            tail->next = node;   // 接到尾部
            tail = node;
        }
    }
    return head;
}

// 合并两个有序链表
Node* merge(Node* L1, Node* L2){
    Node dummy;              // 虚拟头节点
    dummy.next = NULL;
    Node* p = &dummy;        // 新链表尾指针

    while(L1 && L2){
        if(L1->data <= L2->data){
            p->next = L1;
            L1 = L1->next;
        } else {
            p->next = L2;
            L2 = L2->next;
        }
        p = p->next;
    }

    // 接上剩余部分
    p->next = L1 ? L1 : L2;

    return dummy.next;
}

// 输出链表
void printList(Node* head){
    while(head){
        printf("%d ", head->data);
        head = head->next;
    }
}

// 主函数
int main(){
    Node* L1 = createList();
    Node* L2 = createList();

    Node* L3 = merge(L1, L2);

    printList(L3);

    return 0;
}


⸻

❌ 本题错误总结
	1.	while 条件写错

while(L1->next && L2->next) ❌

👉 会漏节点，应为：

while(L1 && L2) ✅



⸻

	2.	忘记连接剩余链表

p->next = L1 ? L1 : L2;



⸻

	3.	返回错误地址

return &dummy; ❌

👉 应返回：

return dummy.next; ✅



⸻

	4.	误用 malloc
👉 合并链表是拼接，不是创建

⸻

⸻

🟢 题目二：删除链表中的重复节点

📌 题目描述

删除链表中重复的节点，保留第一个出现的值。

示例：

输入：
1 2 2 3 3

输出：
1 2 3


⸻

💻 完整代码（可直接运行）

#include <stdio.h>
#include <stdlib.h>

// 定义链表节点
typedef struct Node{
    int data;
    struct Node* next;
} Node;

// 创建链表
Node* createList(){
    Node* head = NULL;
    Node* tail = NULL;
    int x;

    while(scanf("%d", &x) && x != -1){
        Node* node = (Node*)malloc(sizeof(Node));
        node->data = x;
        node->next = NULL;

        if(head == NULL){
            head = tail = node;
        } else {
            tail->next = node;
            tail = node;
        }
    }
    return head;
}

// 删除重复节点（保留第一个）
void deleteNode(Node* head){
    Node* p1 = head;   // 外层指针

    while(p1){
        Node* p2 = p1; // 内层指针

        while(p2->next){
            if(p2->next->data == p1->data){
                // 删除重复节点
                p2->next = p2->next->next;
            } else {
                p2 = p2->next;
            }
        }

        p1 = p1->next;
    }
}

// 输出链表
void printList(Node* head){
    while(head){
        printf("%d ", head->data);
        head = head->next;
    }
}

// 主函数
int main(){
    Node* L1 = createList();

    deleteNode(L1);

    printList(L1);

    return 0;
}


⸻

❌ 本题错误总结
	1.	删除当前节点（错误）

p3->next = p2->next;
p3 = p2; ❌

👉 使用了已删除节点

⸻

	2.	删除后仍然移动指针

p2 = p2->next; ❌

👉 应该：
	•	删除时不动
	•	不删除才移动

⸻

	3.	p2 没重新初始化

Node* p2 = p1;



⸻

	4.	核心理解错误
👉 删除节点后仍使用该节点

⸻

🧠 关键理解

👉 删除节点的本质：

不是删除内存
而是让节点从链表中“不可达”


⸻

🚀 总结
	•	创建链表：必须 malloc
	•	合并链表：拼接节点
	•	删除节点：修改 next
	•	链表本质：从 head 能否访问到该节点

⸻

这版是：

✅ 完整代码
✅ 可运行
✅ 有注释
✅ 有错误总结

👉 直接丢 GitHub 完全OK

⸻

如果你下一步想更狠一点，我可以帮你👇
🔥 做一份「链表八大题模板合集（面试级）」
